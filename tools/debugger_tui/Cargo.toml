[package]
name = "pasm-debugger-tui"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
ratatui = "0.29"
crossterm = "0.29"

[features]
default = []
linked-emulator = []
linked-z80 = ["linked-emulator"]
