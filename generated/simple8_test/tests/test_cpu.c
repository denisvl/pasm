/*
 * Auto-generated test_cpu.c
 * Generated by PASM
 */

#include <stdio.h>
#include <stdlib.h>
#include "Simple8.h"

int main(int argc, char *argv[]) {
    CPUState *cpu = simple8_create(65536);
    if (!cpu) {
        fprintf(stderr, "Failed to create CPU\n");
        return 1;
    }

    if (argc > 1) {
        const char *rom = argv[1];
        if (simple8_load_rom(cpu, rom, 0) != 0) {
            fprintf(stderr, "Failed to load ROM: %s\n", rom);
            simple8_destroy(cpu);
            return 1;
        }
    }

    simple8_run_until(cpu, 100);
    simple8_dump_registers(cpu);
    simple8_destroy(cpu);
    return 0;
}
