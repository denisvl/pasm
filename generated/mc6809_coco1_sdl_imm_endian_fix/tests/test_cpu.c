/*
 * Auto-generated test_cpu.c
 * Generated by PASM
 */

#include <stdio.h>
#include <stdlib.h>
#include "MC6809.h"

int main(int argc, char *argv[]) {
    CPUState *cpu = mc6809_create(65536);
    if (!cpu) {
        fprintf(stderr, "Failed to create CPU\n");
        return 1;
    }

    if (argc > 1) {
        const char *rom = argv[1];
        if (mc6809_load_rom(cpu, rom, 0) != 0) {
            fprintf(stderr, "Failed to load ROM: %s\n", rom);
            mc6809_destroy(cpu);
            return 1;
        }
    }

    mc6809_run_until(cpu, 100);
    mc6809_dump_registers(cpu);
    mc6809_destroy(cpu);
    return 0;
}
