/*
 * Auto-generated test_cpu.c
 * Generated by PASM
 */

#include <stdio.h>
#include <stdlib.h>
#include "MOS6502.h"

int main(int argc, char *argv[]) {
    CPUState *cpu = mos6502_create(65536);
    if (!cpu) {
        fprintf(stderr, "Failed to create CPU\n");
        return 1;
    }

    if (argc > 1) {
        const char *rom = argv[1];
        if (mos6502_load_rom(cpu, rom, 0) != 0) {
            fprintf(stderr, "Failed to load ROM: %s\n", rom);
            mos6502_destroy(cpu);
            return 1;
        }
    }

    mos6502_run_until(cpu, 100);
    mos6502_dump_registers(cpu);
    mos6502_destroy(cpu);
    return 0;
}
