/*
 * Auto-generated test_cpu.c
 * Generated by PASM
 */

#include <stdio.h>
#include <stdlib.h>
#include "Minimal8.h"

int main(int argc, char *argv[]) {
    CPUState *cpu = minimal8_create(65536);
    if (!cpu) {
        fprintf(stderr, "Failed to create CPU\n");
        return 1;
    }

    if (argc > 1) {
        const char *rom = argv[1];
        if (minimal8_load_rom(cpu, rom, 0) != 0) {
            fprintf(stderr, "Failed to load ROM: %s\n", rom);
            minimal8_destroy(cpu);
            return 1;
        }
    }

    minimal8_run_until(cpu, 100);
    minimal8_dump_registers(cpu);
    minimal8_destroy(cpu);
    return 0;
}
