metadata:
  id: host_atari2600
  type: host_adapter
  model: atari2600_hal_headless
  version: "1.0"


state:
  - name: joy1
    type: uint8_t
    initial: "0x00"
  - name: joy2
    type: uint8_t
    initial: "0x00"
  - name: console_state
    type: uint8_t
    initial: "0xFF"
  - name: frame_count
    type: uint32_t
    initial: "0"
  - name: audio_samples
    type: uint32_t
    initial: "0"

interfaces:
  callbacks:
    - name: read_joy1
      args: []
      returns: u8
    - name: read_joy2
      args: []
      returns: u8
    - name: read_console
      args: []
      returns: u8
  handlers:
    - name: video_frame
      args: [u32, u64, u32, u32]
    - name: audio_pcm
      args: [u8, u64]
  signals: []

behavior:
  snippets: {}
  callback_handlers:
    read_joy1: |
      return (uint64_t)comp->joy1;
    read_joy2: |
      return (uint64_t)comp->joy2;
    read_console: |
      return (uint64_t)comp->console_state;
  handler_bodies:
    video_frame: |
      if (argc > 0) comp->frame_count = (uint32_t)(args[0] & 0xFFFFFFFFu);
    audio_pcm: |
      comp->audio_samples += 1u;

coding:
  headers: []
  include_paths: []
  linked_libraries: []
  library_paths: []
