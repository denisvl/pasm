metadata:
  id: host_atari800xl
  type: host_adapter
  model: atari800xl_hal_interactive
  version: '1.0'
state:
- name: host_inited
  type: uint8_t
  initial: '0'
- name: window
  type: void *
  initial: 'NULL'
- name: renderer
  type: void *
  initial: 'NULL'
- name: texture
  type: void *
  initial: 'NULL'
- name: display_shader
  type: void *
  initial: 'NULL'
- name: display_shader_supported
  type: int
  initial: '-1'
- name: texture_w
  type: uint32_t
  initial: '320'
- name: texture_h
  type: uint32_t
  initial: '192'
- name: audio_out_dev
  type: uint32_t
  initial: '0'
- name: audio_out_ready
  type: uint8_t
  initial: '0'
- name: audio_device_samples
  type: uint32_t
  initial: '0'
- name: frame_count
  type: uint32_t
  initial: '0'
- name: audio_samples
  type: uint32_t
  initial: '0'
- name: audio_level
  type: int16_t
  initial: '0'
- name: audio_hp_prev_in
  type: int32_t
  initial: '0'
- name: audio_hp_prev_out
  type: int32_t
  initial: '0'
- name: audio_last_cycle
  type: uint64_t
  initial: '0'
- name: audio_sample_cursor
  type: uint64_t
  initial: '0'
- name: audio_rate
  type: uint32_t
  initial: '0'
- name: audio_channels
  type: uint32_t
  initial: '0'
- name: audio_ring
  type: int16_t *
  initial: 'NULL'
- name: audio_ring_capacity
  type: uint32_t
  initial: '0'
- name: audio_ring_read_idx
  type: uint32_t
  initial: '0'
- name: audio_ring_write_idx
  type: uint32_t
  initial: '0'
- name: audio_ring_fill
  type: uint32_t
  initial: '0'
- name: key_ascii
  type: uint8_t
  initial: '0'
- name: key_mods
  type: uint8_t
  initial: '0'
- name: key_queue
  type: uint8_t *
  initial: 'NULL'
- name: key_q_read_idx
  type: uint8_t
  initial: '0'
- name: key_q_write_idx
  type: uint8_t
  initial: '0'
- name: consol_state
  type: uint8_t
  initial: '0x07'
- name: joy0_state
  type: uint8_t
  initial: '0x0F'
- name: joy1_state
  type: uint8_t
  initial: '0x0F'
- name: trig0
  type: uint8_t
  initial: '1'
- name: trig1
  type: uint8_t
  initial: '1'
- name: trig2
  type: uint8_t
  initial: '1'
- name: trig3
  type: uint8_t
  initial: '1'
- name: break_pressed
  type: uint8_t
  initial: '0'
- name: last_event_poll_cycle
  type: uint64_t
  initial: 0u
- name: overlay_last_ms
  type: uint32_t
  initial: '0'
- name: overlay_last_frame_count
  type: uint64_t
  initial: '0'
- name: overlay_last_cycle_count
  type: uint64_t
  initial: '0'
- name: overlay_fps_x100
  type: uint32_t
  initial: '0'
- name: overlay_cpu_hz
  type: uint64_t
  initial: '0'
- name: overlay_cpu_pct_x10
  type: uint32_t
  initial: '0'
interfaces:
  callbacks:
  - name: keyboard_ascii
    args: []
    returns: u8
  - name: keyboard_mods
    args: []
    returns: u8
  - name: break_state
    args: []
    returns: u8
  - name: consol_state
    args: []
    returns: u8
  - name: joystick_porta
    args: []
    returns: u8
  - name: trigger0
    args: []
    returns: u8
  - name: trigger1
    args: []
    returns: u8
  - name: trigger2
    args: []
    returns: u8
  - name: trigger3
    args: []
    returns: u8
  handlers:
  - name: video_frame
    args:
    - u32
    - u64
    - u32
    - u32
  - name: audio_pcm
    args:
    - u8
    - u64
  signals: []
behavior:
  snippets:
    init: "do {\n    if (comp->host_inited != 0u) break;\n    {\n        const char *disable_dead = cpu_host_hal_getenv(\"\
      PASM_DISABLE_DEADKEYS\");\n        if (disable_dead == NULL || disable_dead[0] != '0') {\n            #if defined(__linux__)\n\
      \            /* Keep scancode flow deterministic by disabling XIM dead-key composition for this process. */\n      \
      \      setenv(\"XMODIFIERS\", \"@im=none\", 1);\n            setenv(\"GTK_IM_MODULE\", \"xim\", 0);\n            setenv(\"\
      QT_IM_MODULE\", \"xim\", 0);\n            #endif\n        }\n    }\n    if (cpu_host_hal_init(CPU_HOST_INIT_VIDEO |\
      \ CPU_HOST_INIT_AUDIO | CPU_HOST_INIT_EVENTS) != 0) break;\n    cpu_host_hal_stop_text_input();\n    int window_w =\
      \ 320;\n    int window_h = 192;\n    cpu_host_hal_display_window_size(\n        cpu->comp_tv.display_width,\n      \
      \  cpu->comp_tv.display_height,\n        cpu->comp_tv.aspect_width,\n        cpu->comp_tv.aspect_height,\n        cpu->comp_tv.display_inches,\n\
      \        320,\n        192,\n        &window_w,\n        &window_h\n    );\n    comp->window = cpu_host_hal_create_window(\n\
      \        \"PASM Atari 800XL\",\n        CPU_HOST_WINDOWPOS_CENTERED,\n        CPU_HOST_WINDOWPOS_CENTERED,\n       \
      \ window_w,\n        window_h,\n        CPU_HOST_WINDOW_RESIZABLE\n    );\n    if (comp->window == NULL) break;\n  \
      \  comp->renderer = cpu_host_hal_create_renderer(comp->window, -1, CPU_HOST_RENDERER_ACCELERATED);\n    if (comp->renderer\
      \ == NULL) {\n        comp->renderer = cpu_host_hal_create_renderer(comp->window, -1, 0);\n    }\n    if (comp->renderer\
      \ == NULL) break;\n\n    {\n        static const char *atari800xl_crt_tv_vertex_shader =\n            \"#version 330\
      \ core\\n\"\n            \"layout (location = 0) in vec3 aPos;\\n\"\n            \"layout (location = 1) in vec2 aTexCoords;\\\
      n\"\n            \"out vec2 TexCoords;\\n\"\n            \"void main() {\\n\"\n            \"    TexCoords = aTexCoords;\\\
      n\"\n            \"    gl_Position = vec4(aPos, 1.0);\\n\"\n            \"}\\n\";\n        static const char *atari800xl_crt_tv_fragment_shader\
      \ =\n            \"#version 330 core\\n\"\n            \"out vec4 FragColor;\\n\"\n            \"in vec2 TexCoords;\\\
      n\"\n            \"uniform sampler2D screenTexture;\\n\"\n            \"uniform vec2 sourceResolution;\\n\"\n      \
      \      \"const float CURVATURE = 8.0;\\n\"\n            \"const float SCANLINE_WEIGHT = 0.10;\\n\"\n            \"const\
      \ float MASK_DARKNESS = 0.08;\\n\"\n            \"const float GLOW_FACTOR = 0.05;\\n\"\n            \"vec2 curve(vec2\
      \ coord) {\\n\"\n            \"    coord = coord * 2.0 - 1.0;\\n\"\n            \"    coord.x *= 1.0 + pow((abs(coord.y)\
      \ / CURVATURE), 2.0);\\n\"\n            \"    coord.y *= 1.0 + pow((abs(coord.x) / CURVATURE), 2.0);\\n\"\n        \
      \    \"    return coord * 0.5 + 0.5;\\n\"\n            \"}\\n\"\n            \"void main() {\\n\"\n            \"  \
      \  vec2 crtUV = curve(TexCoords);\\n\"\n            \"    if (crtUV.x < 0.0 || crtUV.x > 1.0 || crtUV.y < 0.0 || crtUV.y\
      \ > 1.0) {\\n\"\n            \"        FragColor = vec4(0.0, 0.0, 0.0, 1.0);\\n\"\n            \"        return;\\n\"\
      \n            \"    }\\n\"\n            \"    vec3 baseColor = texture(screenTexture, crtUV).rgb;\\n\"\n           \
      \ \"    vec3 blurColor = texture(screenTexture, crtUV + vec2(1.0 / sourceResolution.x, 0.0)).rgb;\\n\"\n           \
      \ \"    baseColor = mix(baseColor, blurColor, GLOW_FACTOR);\\n\"\n            \"    float scan = 0.5 + 0.5 * sin(crtUV.y\
      \ * sourceResolution.y * 3.14159265);\\n\"\n            \"    baseColor *= 1.0 - (SCANLINE_WEIGHT * (1.0 - scan));\\\
      n\"\n            \"    float maskPhase = mod(gl_FragCoord.x + floor(gl_FragCoord.y * 0.5), 3.0);\\n\"\n            \"\
      \    float mask = 1.0 - (MASK_DARKNESS * step(maskPhase, 0.5));\\n\"\n            \"    baseColor *= mask;\\n\"\n  \
      \          \"    baseColor *= 1.08;\\n\"\n            \"    FragColor = vec4(baseColor, 1.0);\\n\"\n            \"}\\\
      n\";\n        comp->display_shader = cpu_host_hal_shader_create(comp->renderer, atari800xl_crt_tv_vertex_shader, atari800xl_crt_tv_fragment_shader);\n\
      \        comp->display_shader_supported = (comp->display_shader != NULL) ? -1 : 0;\n    }\n    comp->texture = cpu_host_hal_create_texture(\n\
      \        comp->renderer,\n        CPU_HOST_PIXELFORMAT_ARGB8888,\n        CPU_HOST_TEXTUREACCESS_STREAMING,\n      \
      \  320,\n        192\n    );\n    if (comp->texture == NULL) break;\n    if (comp->key_queue == NULL) {\n        comp->key_queue\
      \ = (uint8_t *)cpu_host_hal_alloc(16u * sizeof(uint8_t));\n        if (comp->key_queue == NULL) break;\n        cpu_host_hal_memset(comp->key_queue,\
      \ 0, 16u * sizeof(uint8_t));\n    }\n    cpu_host_hal_show_window(comp->window);\n    cpu_host_hal_raise_window(comp->window);\n\
      \    (void)cpu_host_hal_set_window_input_focus(comp->window);\n    {\n        const char *audio_env = cpu_host_hal_getenv(\"\
      PASM_HOST_AUDIO\");\n        if (audio_env == NULL || audio_env[0] != '0') {\n            if (cpu_host_hal_init_subsystem(CPU_HOST_INIT_AUDIO)\
      \ != 0) {\n                /* Keep host running without audio if audio backend is unavailable. */\n                comp->audio_out_dev\
      \ = 0u;\n                comp->audio_out_ready = 0u;\n                comp->audio_device_samples = 0u;\n           \
      \         comp->audio_channels = 0u;\n            } else {\n            CPUHostAudioSpec want;\n            CPUHostAudioSpec\
      \ have;\n            cpu_host_audio_spec_zero(&want);\n            cpu_host_audio_spec_zero(&have);\n            want.freq\
      \ = (CPU_AUDIO_SAMPLE_RATE > 0u) ? (int)CPU_AUDIO_SAMPLE_RATE : 44100;\n            want.format = CPU_HOST_AUDIO_FORMAT_S16;\n\
      \            want.channels = 1;\n            want.samples = 512;\n            want.callback = NULL;\n            comp->audio_device_samples\
      \ = 0u;\n                    comp->audio_channels = 0u;\n            comp->audio_out_dev = cpu_host_hal_audio_open(NULL,\
      \ 0, &want, &have, CPU_HOST_AUDIO_ALLOW_CHANNELS_CHANGE |\n                    CPU_HOST_AUDIO_ALLOW_SAMPLES_CHANGE);\n\
      \            if (comp->audio_out_dev != 0u && have.format == CPU_HOST_AUDIO_FORMAT_S16 && have.channels <= 2u && have.channels\
      \ >= 1u) {\n                uint32_t ring_capacity = (have.freq > 0) ? ((uint32_t)have.freq * 2u) : 88200u;\n      \
      \          if (ring_capacity < 8192u) ring_capacity = 8192u;\n                comp->audio_ring = (int16_t *)cpu_host_hal_alloc((size_t)ring_capacity\
      \ * sizeof(int16_t));\n                if (comp->audio_ring != NULL) {\n                    comp->audio_rate = (have.freq\
      \ > 0) ? (uint32_t)have.freq : 44100u;\n                    comp->audio_ring_capacity = ring_capacity;\n           \
      \         comp->audio_ring_read_idx = 0u;\n                    comp->audio_ring_write_idx = 0u;\n                  \
      \  comp->audio_ring_fill = 0u;\n                    comp->audio_device_samples = (uint32_t)have.samples * (uint32_t)have.channels;\n\
      \                    comp->audio_channels = (uint32_t)have.channels;\n                    comp->audio_out_ready = 1u;\n\
      \                    cpu_host_hal_audio_pause(comp->audio_out_dev, 0);\n                } else {\n                 \
      \   cpu_host_hal_audio_close(comp->audio_out_dev);\n                    comp->audio_out_dev = 0u;\n                \
      \    comp->audio_device_samples = 0u;\n                    comp->audio_channels = 0u;\n                }\n         \
      \   } else if (comp->audio_out_dev != 0u) {\n                cpu_host_hal_audio_close(comp->audio_out_dev);\n      \
      \          comp->audio_out_dev = 0u;\n                comp->audio_device_samples = 0u;\n                    comp->audio_channels\
      \ = 0u;\n            }\n            }\n        }\n    }\n    comp->host_inited = 1u;\n} while (0);\n"
    reset: "cpu->debug_overlay_enabled = true;\ncomp->frame_count = 0u;\ncomp->audio_samples = 0u;\ncomp->audio_level = 0;\ncomp->audio_hp_prev_in = 0;\ncomp->audio_hp_prev_out\
      \ = 0;\ncomp->audio_last_cycle = 0u;\ncomp->audio_sample_cursor = 0u;\ncomp->audio_ring_read_idx = 0u;\ncomp->audio_ring_write_idx\
      \ = 0u;\ncomp->audio_ring_fill = 0u;\ncomp->key_ascii = 0u;\ncomp->key_mods = 0u;\nif (comp->key_queue != NULL) {\n    memset(comp->key_queue,\
      \ 0, 16u * sizeof(uint8_t));\n}\ncomp->key_q_read_idx = 0u;\ncomp->key_q_write_idx = 0u;\ncomp->consol_state = 0x07u;\n\
      comp->joy0_state = 0x0Fu;\ncomp->joy1_state = 0x0Fu;\ncomp->trig0 = 1u;\ncomp->trig1 = 1u;\ncomp->trig2 = 1u;\ncomp->trig3\
      \ = 1u;\ncomp->last_event_poll_cycle = 0u;\ncomp->overlay_last_ms = 0u;\ncomp->overlay_last_frame_count = 0u;\ncomp->overlay_last_cycle_count\
      \ = 0u;\ncomp->overlay_fps_x100 = 0u;\ncomp->overlay_cpu_hz = 0u;\ncomp->overlay_cpu_pct_x10 = 0u;\nif (comp->audio_out_dev\
      \ != 0u) {\n    cpu_host_hal_audio_clear(comp->audio_out_dev);\n}\n"
    step_post: "if (comp->host_inited == 0u) return;\n{\n    uint64_t now = cpu->total_cycles;\n    if ((now - comp->last_event_poll_cycle)\
      \ < 4096u) {\n        return;\n    }\n    comp->last_event_poll_cycle = now;\n}\nCPUHostEvent ev;\nwhile (cpu_host_hal_poll_event(&ev))\
      \ {\n    if (\n        cpu_host_hal_event_type(&ev) == CPU_HOST_EVENT_KEYDOWN ||\n        cpu_host_hal_event_type(&ev)\
      \ == CPU_HOST_EVENT_KEYUP\n    ) {\n        int32_t sc_ev = cpu_host_hal_event_scancode(&ev);\n        uint8_t is_down_ev\
      \ = (cpu_host_hal_event_type(&ev) == CPU_HOST_EVENT_KEYDOWN) ? 1u : 0u;\n        const char *kb_trace = cpu_host_hal_getenv(\"PASM_ATARI800XL_KB_EVENTS\");\n\
      \        if (kb_trace != NULL && kb_trace[0] == '1') {\n            int32_t key_ev = cpu_host_hal_event_keycode(&ev);\n            cpu_host_hal_log(\"atari800xl_kb_event cycle=%llu sc=%d sc_name=%s key=%d key_name=%s down=%u repeat=%u\",\n\
      \                (unsigned long long)cpu->total_cycles,\n                (int)sc_ev,\n                cpu_host_hal_scancode_name(sc_ev),\n                (int)key_ev,\n                cpu_host_hal_key_name(key_ev),\n                (unsigned)is_down_ev,\n\
      \                (unsigned)cpu_host_hal_event_key_repeat(&ev));\n        }\n    }\n    if (cpu_host_hal_event_type(&ev)\
      \ == CPU_HOST_EVENT_QUIT) {\n        cpu->running = false;\n        cpu->halted = true;\n        return;\n    } else\
      \ if (cpu_host_hal_event_type(&ev) == CPU_HOST_EVENT_KEYDOWN && cpu_host_hal_event_key_repeat(&ev) == 0) {\n       \
      \ int32_t sc = cpu_host_hal_event_scancode(&ev);\n        /* Discover special scancodes from the runtime map by mapper_key_id\
      \ (layout independent). */\n        {\n            static int32_t break_sc = -2;\n            if (break_sc == -2) {\n\
      \                break_sc = -1;\n                for (size_t i = 0; i < g_runtime_keyboard_map.binding_count; ++i) {\n\
      \                    const RuntimeKeyboardBinding *b = &g_runtime_keyboard_map.bindings[i];\n                    if\
      \ (b->mapper_key_id[0] == '\\0') continue;\n                    if (strcmp(b->mapper_key_id, \"key_break\") == 0) break_sc\
      \ = b->scancode;\n                }\n            }\n            if (break_sc >= 0 && sc == break_sc) {\n           \
      \     comp->break_pressed = 1u;\n                continue;\n            }\n        }\n        if (sc == CPU_HOST_SCANCODE(F2) || sc == CPU_HOST_SCANCODE(HOME))\
      \ {\n            comp->consol_state = (uint8_t)(comp->consol_state & (uint8_t)~0x04u); /* OPTION */\n            continue;\n\
      \        }\n        if (sc == CPU_HOST_SCANCODE(F3) || sc == CPU_HOST_SCANCODE(END)) {\n            comp->consol_state = (uint8_t)(comp->consol_state\
      \ & (uint8_t)~0x02u); /* SELECT */\n            continue;\n        }\n        if (sc == CPU_HOST_SCANCODE(F4) || sc == CPU_HOST_SCANCODE(PAGEUP)) {\n \
      \           comp->consol_state = (uint8_t)(comp->consol_state & (uint8_t)~0x01u); /* START */\n            continue;\n\
      \        }\n        if (sc == CPU_HOST_SCANCODE(UP) || sc == CPU_HOST_SCANCODE(KP_8)) {\n            comp->joy0_state\
      \ = (uint8_t)(comp->joy0_state & (uint8_t)~0x01u);\n            continue;\n        }\n        if (sc == CPU_HOST_SCANCODE(DOWN)\
      \ || sc == CPU_HOST_SCANCODE(KP_2)) {\n            comp->joy0_state = (uint8_t)(comp->joy0_state & (uint8_t)~0x02u);\n\
      \            continue;\n        }\n        if (sc == CPU_HOST_SCANCODE(LEFT) || sc == CPU_HOST_SCANCODE(KP_4)) {\n \
      \           comp->joy0_state = (uint8_t)(comp->joy0_state & (uint8_t)~0x04u);\n            continue;\n        }\n  \
      \      if (sc == CPU_HOST_SCANCODE(RIGHT) || sc == CPU_HOST_SCANCODE(KP_6)) {\n            comp->joy0_state = (uint8_t)(comp->joy0_state\
      \ & (uint8_t)~0x08u);\n            continue;\n        }\n        if (\n            sc == CPU_HOST_SCANCODE(RCTRL) ||\n\
      \            sc == CPU_HOST_SCANCODE(RALT) ||\n            sc == CPU_HOST_SCANCODE(KP_0)\n        ) {\n            comp->trig0\
      \ = 0u;\n            continue;\n        }\n        if (sc == CPU_HOST_SCANCODE(I)) {\n            comp->joy1_state =\
      \ (uint8_t)(comp->joy1_state & (uint8_t)~0x01u);\n        }\n        if (sc == CPU_HOST_SCANCODE(K)) {\n           \
      \ comp->joy1_state = (uint8_t)(comp->joy1_state & (uint8_t)~0x02u);\n        }\n        if (sc == CPU_HOST_SCANCODE(J))\
      \ {\n            comp->joy1_state = (uint8_t)(comp->joy1_state & (uint8_t)~0x04u);\n        }\n        if (sc == CPU_HOST_SCANCODE(L))\
      \ {\n            comp->joy1_state = (uint8_t)(comp->joy1_state & (uint8_t)~0x08u);\n        }\n        if (sc == CPU_HOST_SCANCODE(LCTRL))\
      \ {\n            comp->trig1 = 0u;\n            continue;\n        }\n        if (sc == CPU_HOST_SCANCODE(TAB)) {\n\
      \            comp->trig2 = 0u;\n            continue;\n        }\n        {\n            int key_count = 0;\n      \
      \      const uint8_t *ks = cpu_host_hal_keyboard_state(&key_count);\n            uint8_t shifted = 0u;\n           \
      \ uint8_t controlled = 0u;\n            const RuntimeKeyboardBinding *matched = NULL;\n            if (ks != NULL &&\
      \ key_count > 0) {\n                size_t kcount = (size_t)key_count;\n                for (size_t i = 0; i < g_runtime_keyboard_map.binding_count;\
      \ ++i) {\n                    const RuntimeKeyboardBinding *m = &g_runtime_keyboard_map.bindings[i];\n             \
      \       if (m->scancode < 0 || (size_t)m->scancode >= kcount) continue;\n                    if (ks[m->scancode] ==\
      \ 0u) continue;\n                    if (m->is_shift_modifier != 0u) shifted = 1u;\n                    if (m->is_ctrl_modifier\
      \ != 0u) controlled = 1u;\n                }\n                for (size_t i = 0; i < g_runtime_keyboard_map.binding_count;\
      \ ++i) {\n                    const RuntimeKeyboardBinding *b = &g_runtime_keyboard_map.bindings[i];\n             \
      \       if (b->scancode == sc) { matched = b; break; }\n                }\n            }\n            if (matched !=\
      \ NULL) {\n                uint8_t out = 0u;\n                if (controlled != 0u && matched->has_ascii_ctrl != 0u) out = matched->ascii_ctrl;\n                else if (shifted != 0u && matched->has_ascii_shift != 0u) out = matched->ascii_shift;\n                else if (matched->has_ascii != 0u) out = matched->ascii;\n                cpu_component_keyboard_ascii_queue_push(out);\n\
      \            }\n        }\n    } else if (cpu_host_hal_event_type(&ev) == CPU_HOST_EVENT_KEYUP) {\n        int32_t sc\
      \ = cpu_host_hal_event_scancode(&ev);\n        /* Clear special-key pressed state on release. */\n        {\n      \
      \      static int32_t break_sc = -2;\n            if (break_sc == -2) {\n                break_sc = -1;\n          \
      \      for (size_t i = 0; i < g_runtime_keyboard_map.binding_count; ++i) {\n                    const RuntimeKeyboardBinding\
      \ *b = &g_runtime_keyboard_map.bindings[i];\n                    if (b->mapper_key_id[0] == '\\0') continue;\n     \
      \               if (strcmp(b->mapper_key_id, \"key_break\") == 0) break_sc = b->scancode;\n                }\n     \
      \       }\n            if (break_sc >= 0 && sc == break_sc) {\n                comp->break_pressed = 0u;\n         \
      \   }\n        }\n        if (sc == CPU_HOST_SCANCODE(F2) || sc == CPU_HOST_SCANCODE(HOME)) {\n            comp->consol_state = (uint8_t)(comp->consol_state\
      \ | 0x04u);\n        } else if (sc == CPU_HOST_SCANCODE(F3) || sc == CPU_HOST_SCANCODE(END)) {\n            comp->consol_state = (uint8_t)(comp->consol_state\
      \ | 0x02u);\n        } else if (sc == CPU_HOST_SCANCODE(F4) || sc == CPU_HOST_SCANCODE(PAGEUP)) {\n            comp->consol_state = (uint8_t)(comp->consol_state\
      \ | 0x01u);\n        }\n    }\n}\n{\n    int key_count = 0;\n    const uint8_t *ks = cpu_host_hal_keyboard_state(&key_count);\n    uint8_t shifted = 0u;\n    uint8_t controlled = 0u;\n    uint8_t any_key = 0u;\n    uint8_t live_out = 0u;\n    if (ks != NULL && key_count > 0) {\n        size_t kcount = (size_t)key_count;\n        for (size_t i = 0; i < g_runtime_keyboard_map.binding_count; ++i) {\n            const RuntimeKeyboardBinding *b = &g_runtime_keyboard_map.bindings[i];\n            if (b->scancode < 0 || (size_t)b->scancode >= kcount) continue;\n            if (ks[b->scancode] == 0u) continue;\n            if (b->is_shift_modifier != 0u) shifted = 1u;\n            if (b->is_ctrl_modifier != 0u) controlled = 1u;\n        }\n        for (size_t i = 0; i < g_runtime_keyboard_map.binding_count; ++i) {\n            const RuntimeKeyboardBinding *b = &g_runtime_keyboard_map.bindings[i];\n            if (b->scancode < 0 || (size_t)b->scancode >= kcount) continue;\n            if (ks[b->scancode] == 0u) continue;\n            if (b->is_shift_modifier != 0u || b->is_ctrl_modifier != 0u) continue;\n            if (b->has_ascii != 0u || b->has_ascii_shift != 0u || b->has_ascii_ctrl != 0u) {\n                uint8_t out = 0u;\n                any_key = 1u;\n                if (controlled != 0u && b->has_ascii_ctrl != 0u) out = b->ascii_ctrl;\n                else if (shifted != 0u && b->has_ascii_shift != 0u) out = b->ascii_shift;\n                else if (b->has_ascii != 0u) out = b->ascii;\n                if (live_out == 0u && out != 0u) {\n                    /* The runtime map already stores Atari KBCODE + 1. */\n                    live_out = out;\n                }\n            }\n        }\n    }\n    comp->key_ascii = live_out;\n    comp->key_mods = (uint8_t)((shifted ? 0x01u : 0u) | (controlled ? 0x02u : 0u) | (any_key ? 0x04u : 0u));\n}\n/* Optional controller map: when present, it drives joystick/trigger state (host-abstracted).\
      \ */\nif (g_runtime_controller_map.loaded != 0u) {\n    uint8_t focus = (comp->window != NULL) ? cpu_host_hal_window_has_focus(comp->window)\
      \ : 1u;\n    uint8_t j0 = 0x0Fu;\n    uint8_t j1 = 0x0Fu;\n    uint8_t t0 = 1u;\n    uint8_t t1 = 1u;\n    cpu_component_controller_map_poll(cpu,\
      \ focus);\n    if (cpu_component_controller_pressed(\"P1_UP\")) j0 = (uint8_t)(j0 & (uint8_t)~0x01u);\n    if (cpu_component_controller_pressed(\"\
      P1_DOWN\")) j0 = (uint8_t)(j0 & (uint8_t)~0x02u);\n    if (cpu_component_controller_pressed(\"P1_LEFT\")) j0 = (uint8_t)(j0\
      \ & (uint8_t)~0x04u);\n    if (cpu_component_controller_pressed(\"P1_RIGHT\")) j0 = (uint8_t)(j0 & (uint8_t)~0x08u);\n\
      \    if (cpu_component_controller_pressed(\"P1_BUTTON0\")) t0 = 0u;\n    if (cpu_component_controller_pressed(\"P2_UP\"\
      )) j1 = (uint8_t)(j1 & (uint8_t)~0x01u);\n    if (cpu_component_controller_pressed(\"P2_DOWN\")) j1 = (uint8_t)(j1 &\
      \ (uint8_t)~0x02u);\n    if (cpu_component_controller_pressed(\"P2_LEFT\")) j1 = (uint8_t)(j1 & (uint8_t)~0x04u);\n\
      \    if (cpu_component_controller_pressed(\"P2_RIGHT\")) j1 = (uint8_t)(j1 & (uint8_t)~0x08u);\n    if (cpu_component_controller_pressed(\"\
      P2_BUTTON0\")) t1 = 0u;\n    comp->joy0_state = j0;\n    comp->joy1_state = j1;\n    comp->trig0 = t0;\n    comp->trig1\
      \ = t1;\n}\nif (comp->audio_out_dev != 0u && comp->audio_out_ready != 0u && comp->audio_ring\
      \ != NULL && comp->audio_ring_fill > 0u) {\n    uint32_t channels = (comp->audio_channels > 0u) ? comp->audio_channels\
      \ : 1u;\n    if (channels > 2u) channels = 2u;\n    uint32_t queued_samples = cpu_host_hal_audio_queued_bytes(comp->audio_out_dev)\
      \ / (uint32_t)sizeof(int16_t);\n    uint32_t queued_frames = queued_samples / channels;\n    uint32_t device_samples\
      \ = (comp->audio_device_samples > 0u) ? comp->audio_device_samples : 512u;\n    uint32_t device_frames = device_samples\
      \ / channels;\n    if (device_frames == 0u) device_frames = 512u;\n    uint32_t low_watermark = device_frames * 2u;\n\
      \    uint32_t target_total = device_frames * 4u;\n    uint32_t max_total = device_frames * 8u;\n    if (queued_frames\
      \ + comp->audio_ring_fill > max_total) {\n        uint32_t desired_ring_fill = (queued_frames < target_total) ? (target_total\
      \ - queued_frames) : 0u;\n        while (comp->audio_ring_fill > desired_ring_fill) {\n            comp->audio_ring_read_idx\
      \ += 1u;\n            if (comp->audio_ring_read_idx >= comp->audio_ring_capacity) comp->audio_ring_read_idx = 0u;\n\
      \            comp->audio_ring_fill -= 1u;\n        }\n    }\n    if (queued_frames < low_watermark) {\n        uint32_t\
      \ desired = (queued_frames < target_total) ? (target_total - queued_frames) : 0u;\n        uint32_t to_send = (comp->audio_ring_fill\
      \ < desired) ? comp->audio_ring_fill : desired;\n        while (to_send > 0u) {\n            int16_t chunk[1024];\n\
      \            uint32_t max_frames = 1024u / channels;\n            uint32_t n = (to_send > max_frames) ? max_frames :\
      \ to_send;\n            uint32_t out = 0u;\n            if (n == 0u) break;\n            for (uint32_t i = 0u; i < n;\
      \ ++i) {\n                int16_t sample = comp->audio_ring[comp->audio_ring_read_idx];\n                for (uint32_t\
      \ ch = 0u; ch < channels; ++ch) {\n                    chunk[out++] = sample;\n                }\n                comp->audio_ring_read_idx\
      \ += 1u;\n                if (comp->audio_ring_read_idx >= comp->audio_ring_capacity) comp->audio_ring_read_idx = 0u;\n\
      \            }\n            comp->audio_ring_fill -= n;\n            if (cpu_host_hal_audio_queue(comp->audio_out_dev,\
      \ chunk, out * (uint32_t)sizeof(int16_t)) != 0) {\n                break;\n            }\n            to_send -= n;\n\
      \        }\n    }\n}\n"
    destroy: "if (comp->audio_out_dev != 0u) {\n    cpu_host_hal_audio_close(comp->audio_out_dev);\n    comp->audio_out_dev\
      \ = 0u;\n}\ncomp->audio_device_samples = 0u;\ncomp->audio_channels = 0u;\nif (comp->audio_ring != NULL) {\n    cpu_host_hal_free(comp->audio_ring);\n\
      \    comp->audio_ring = NULL;\n}\nif (comp->key_queue != NULL) {\n    cpu_host_hal_free(comp->key_queue);\n    comp->key_queue\
      \ = NULL;\n}\ncomp->audio_ring_capacity = 0u;\ncomp->audio_ring_read_idx = 0u;\ncomp->audio_ring_write_idx = 0u;\ncomp->audio_ring_fill\
      \ = 0u;\nif (comp->display_shader != NULL) {\n    cpu_host_hal_shader_destroy(comp->display_shader);\n    comp->display_shader\
      \ = NULL;\n}\ncomp->display_shader_supported = -1;\nif (comp->texture != NULL) {\n    cpu_host_hal_destroy_texture(comp->texture);\n\
      \    comp->texture = NULL;\n}\nif (comp->renderer != NULL) {\n    cpu_host_hal_destroy_renderer(comp->renderer);\n \
      \   comp->renderer = NULL;\n}\nif (comp->window != NULL) {\n    cpu_host_hal_destroy_window(comp->window);\n    comp->window\
      \ = NULL;\n}\nif (comp->host_inited != 0u) {\n    cpu_host_hal_quit_subsystems();\n    cpu_host_hal_quit();\n    comp->host_inited\
      \ = 0u;\n}\n"
  callback_handlers:
    keyboard_ascii: '{
      static uint32_t auto_boot_last_return_frame = 0u;
      static uint32_t auto_boot_last_space_frame = 0u;
      uint8_t queued = cpu_component_keyboard_ascii_queue_pop();
      const char *auto_boot = cpu_host_hal_getenv("PASM_CASSETTE_AUTO_BOOT");
      if (comp->frame_count == 0u) {
          auto_boot_last_return_frame = 0u;
          auto_boot_last_space_frame = 0u;
      }
      if (auto_boot != NULL && auto_boot[0] == ''1'') {
          if (comp->frame_count >= 1u && (auto_boot_last_return_frame == 0u || (comp->frame_count - auto_boot_last_return_frame) >= 24u) && comp->frame_count <= 240u) {
              cpu_component_keyboard_ascii_queue_push(13u); /* RETURN KBCODE+1 */
              auto_boot_last_return_frame = comp->frame_count;
          }
          if (comp->frame_count >= 48u && (auto_boot_last_space_frame == 0u || (comp->frame_count - auto_boot_last_space_frame) >= 48u) && comp->frame_count <= 240u) {
              cpu_component_keyboard_ascii_queue_push(34u); /* SPACE KBCODE+1 */
              auto_boot_last_space_frame = comp->frame_count;
          }
          queued = cpu_component_keyboard_ascii_queue_pop();
      }
      if (queued != 0u) return (uint64_t)queued;
      return (uint64_t)comp->key_ascii;
      }

      '
    keyboard_mods: 'return (uint64_t)comp->key_mods;

      '
    break_state: 'return (uint64_t)(comp->break_pressed & 0x01u);

      '
    consol_state: '{
      static uint8_t auto_boot_reads = 0u;
      static int trace_init = -1;
      static FILE *trace_fp = NULL;
      static uint32_t trace_count = 0u;
      uint8_t result;
      const char *auto_boot = cpu_host_hal_getenv("PASM_CASSETTE_AUTO_BOOT");
      const char *auto_start = cpu_host_hal_getenv("PASM_ATARI800XL_AUTO_START");
      if (comp->frame_count == 0u) auto_boot_reads = 0u;
      if (trace_init < 0) {
          const char *env = cpu_host_hal_getenv("PASM_ATARI800XL_BOOT_TRACE");
          trace_init = (env != NULL && env[0] != ''\0'' && env[0] != ''0'') ? 1 : 0;
          if (trace_init != 0) {
              const char *path = cpu_host_hal_getenv("PASM_ATARI800XL_BOOT_TRACE_FILE");
              if (path != NULL && path[0] != ''\0'') trace_fp = fopen(path, "a");
              if (trace_fp == NULL) trace_fp = stderr;
          }
      }
      if (auto_boot != NULL && auto_boot[0] == ''1'' && auto_boot_reads < 8u) {
          auto_boot_reads += 1u;
          result = 0x02u; /* hold OPTION+START low across early OS console reads */
      } else if (auto_start != NULL && auto_start[0] == ''1'' && auto_boot_reads < 6u) {
          auto_boot_reads += 1u;
          result = 0x06u; /* hold START low across the early OS console reads */
      } else {
          result = (uint8_t)(comp->consol_state & 0x07u);
      }
      if (trace_init != 0 && trace_count < 32u) {
          fprintf(
              trace_fp,
              "boot_trace consol frame=%u read=%u auto_boot=%c auto_start=%c result=%02X\n",
              (unsigned)comp->frame_count,
              (unsigned)auto_boot_reads,
              (auto_boot != NULL && auto_boot[0] != ''\0'') ? auto_boot[0] : ''-'',
              (auto_start != NULL && auto_start[0] != ''\0'') ? auto_start[0] : ''-'',
              (unsigned)result
          );
          fflush(trace_fp);
          trace_count += 1u;
      }
      return (uint64_t)result;
      }

      '
    joystick_porta: 'return (uint64_t)(((comp->joy1_state & 0x0Fu) << 4u) | (comp->joy0_state & 0x0Fu));

      '
    trigger0: 'return (uint64_t)(comp->trig0 & 0x01u);

      '
    trigger1: 'return (uint64_t)(comp->trig1 & 0x01u);

      '
    trigger2: 'return (uint64_t)(comp->trig2 & 0x01u);

      '
    trigger3: 'return (uint64_t)(comp->trig3 & 0x01u);

      '
  handler_bodies:
    video_frame: "if (comp->host_inited == 0u || argc < 4) return;\nvoid *renderer = comp->renderer;\nvoid *texture = comp->texture;\n\
      uint32_t frame = (uint32_t)(args[0] & 0xFFFFFFFFu);\nuint32_t *pixels = (uint32_t *)(uintptr_t)args[1];\nuint32_t logical_w\
      \ = (uint32_t)(args[2] & 0xFFFFFFFFu);\nuint32_t logical_h = (uint32_t)(args[3] & 0xFFFFFFFFu);\nconst uint32_t buf_w\
      \ = 320u;\nconst uint32_t buf_h = 192u;\nif (renderer == NULL || texture == NULL || pixels == NULL || logical_w == 0u\
      \ || logical_h == 0u) return;\ncomp->frame_count = frame;\npasm_overlay_update_perf(\n    cpu_host_hal_ticks_ms(),\n    (uint64_t)frame,\n\
      \    cpu->total_cycles,\n    (uint64_t)CPU_SYSTEM_CLOCK_HZ,\n    &comp->overlay_last_ms,\n    &comp->overlay_last_frame_count,\n\
      \    &comp->overlay_last_cycle_count,\n    &comp->overlay_fps_x100,\n    &comp->overlay_cpu_hz,\n    &comp->overlay_cpu_pct_x10\n);\nif (cpu->debug_overlay_enabled) {\n    pasm_overlay_draw_perf(\n\
      \        pixels,\n        buf_w,\n        buf_h,\n        comp->overlay_fps_x100,\n        comp->overlay_cpu_hz,\n \
      \       comp->overlay_cpu_pct_x10\n    );\n}\nif (comp->texture_w != buf_w || comp->texture_h != buf_h) {\n    cpu_host_hal_destroy_texture(texture);\n\
      \    texture = cpu_host_hal_create_texture(renderer, CPU_HOST_PIXELFORMAT_ARGB8888, CPU_HOST_TEXTUREACCESS_STREAMING,\
      \ (int)buf_w, (int)buf_h);\n    if (texture == NULL) return;\n    comp->texture = (void *)texture;\n    comp->texture_w\
      \ = buf_w;\n    comp->texture_h = buf_h;\n}\nif (cpu_host_hal_update_texture(texture, NULL, pixels, (int)(buf_w * sizeof(uint32_t)))\
      \ != 0) return;\ncpu_host_hal_render_set_draw_color(renderer, 0, 0, 0, 255);\ncpu_host_hal_render_clear(renderer);\n\
      {\n    int ww = 0;\n    int wh = 0;\n    int scaled_w = 0;\n    int scaled_h = 0;\n    CPUHostRect dst;\n    if (cpu_host_hal_renderer_output_size(renderer,\
      \ &ww, &wh) != 0 || ww <= 0 || wh <= 0) return;\n    uint32_t aspect_w = cpu->comp_tv.aspect_width;\n    uint32_t aspect_h\
      \ = cpu->comp_tv.aspect_height;\n    if (aspect_w == 0u || aspect_h == 0u) {\n        aspect_w = buf_w;\n        aspect_h\
      \ = buf_h;\n    }\n    scaled_w = ww;\n    scaled_h = (int)((((int64_t)ww) * (int64_t)aspect_h) / (int64_t)aspect_w);\n\
      \    if (scaled_h > wh) {\n        scaled_h = wh;\n        scaled_w = (int)((((int64_t)wh) * (int64_t)aspect_w) / (int64_t)aspect_h);\n\
      \    }\n    dst.w = (scaled_w > 0) ? scaled_w : ww;\n    dst.h = (scaled_h > 0) ? scaled_h : wh;\n    dst.x = (ww -\
      \ dst.w) / 2;\n    dst.y = (wh - dst.h) / 2;\n    if (comp->display_shader_supported < 0) {\n        comp->display_shader_supported\
      \ = (comp->display_shader != NULL && cpu_host_hal_renderer_supports_shaders(renderer) != 0) ? 1 : 0;\n    }\n    if\
      \ (comp->display_shader_supported != 0) {\n        if (cpu_host_hal_render_copy_shader(renderer, texture, NULL, &dst,\
      \ comp->display_shader, (int)buf_w, (int)buf_h, ww, wh) != 0) {\n            comp->display_shader_supported = 0;\n \
      \           if (cpu_host_hal_render_copy(renderer, texture, NULL, &dst) != 0) return;\n        }\n    } else if (cpu_host_hal_render_copy(renderer,\
      \ texture, NULL, &dst) != 0) return;\n}\ncpu_host_hal_render_present(renderer);\n"
    audio_pcm: "if (comp->audio_out_dev == 0u || comp->audio_out_ready == 0u || comp->audio_ring == NULL || comp->audio_ring_capacity\
      \ == 0u || argc < 1) return;\nuint8_t level_u = (uint8_t)(args[0] & 0xFFu);\nuint64_t cycle = (argc > 1) ? (uint64_t)args[1]\
      \ : cpu->total_cycles;\nuint64_t sample_rate = (comp->audio_rate > 0u) ? (uint64_t)comp->audio_rate : ((CPU_AUDIO_SAMPLE_RATE\
      \ > 0u) ? CPU_AUDIO_SAMPLE_RATE : 44100u);\nuint64_t clock_hz = (CPU_SYSTEM_CLOCK_HZ > 0u) ? CPU_SYSTEM_CLOCK_HZ : 1u;\n\
      uint64_t target_sample_index = (cycle * sample_rate) / clock_hz;\nint32_t sample_in = ((int32_t)((int16_t)(int)level_u\
      \ - 128)) << 8;\nint32_t sample_hp = sample_in - comp->audio_hp_prev_in + ((comp->audio_hp_prev_out * 32440) / 32768);\n\
      int16_t next_level;\nint16_t prev_level = comp->audio_level;\nif (sample_hp < -32768) sample_hp = -32768;\nif (sample_hp\
      \ > 32767) sample_hp = 32767;\nnext_level = (int16_t)sample_hp;\ncomp->audio_hp_prev_in = sample_in;\ncomp->audio_hp_prev_out\
      \ = sample_hp;\n\nif (comp->audio_last_cycle == 0u && comp->audio_sample_cursor == 0u && comp->audio_samples == 0u)\
      \ {\n    comp->audio_last_cycle = cycle;\n    comp->audio_sample_cursor = target_sample_index;\n    comp->audio_level\
      \ = next_level;\n    return;\n}\n\nif (target_sample_index < comp->audio_sample_cursor) {\n    target_sample_index =\
      \ comp->audio_sample_cursor;\n}\n{\n    uint64_t samples_to_emit = target_sample_index - comp->audio_sample_cursor;\n\
      \    while (samples_to_emit > 0u) {\n        uint64_t n = (samples_to_emit > 4096u) ? 4096u : samples_to_emit;\n   \
      \     for (uint64_t i = 0u; i < n; ++i) {\n            if (comp->audio_ring_fill >= comp->audio_ring_capacity) {\n \
      \               comp->audio_ring_read_idx += 1u;\n                if (comp->audio_ring_read_idx >= comp->audio_ring_capacity)\
      \ comp->audio_ring_read_idx = 0u;\n                comp->audio_ring_fill -= 1u;\n            }\n            comp->audio_ring[comp->audio_ring_write_idx]\
      \ = prev_level;\n            comp->audio_ring_write_idx += 1u;\n            if (comp->audio_ring_write_idx >= comp->audio_ring_capacity)\
      \ comp->audio_ring_write_idx = 0u;\n            comp->audio_ring_fill += 1u;\n        }\n        comp->audio_samples\
      \ += (uint32_t)n;\n        comp->audio_sample_cursor += n;\n        samples_to_emit -= n;\n    }\n}\nif (comp->audio_ring_fill\
      \ >= comp->audio_ring_capacity) {\n    comp->audio_ring_read_idx += 1u;\n    if (comp->audio_ring_read_idx >= comp->audio_ring_capacity)\
      \ comp->audio_ring_read_idx = 0u;\n    comp->audio_ring_fill -= 1u;\n}\ncomp->audio_ring[comp->audio_ring_write_idx]\
      \ = next_level;\ncomp->audio_ring_write_idx += 1u;\nif (comp->audio_ring_write_idx >= comp->audio_ring_capacity) comp->audio_ring_write_idx\
      \ = 0u;\ncomp->audio_ring_fill += 1u;\n\ncomp->audio_last_cycle = cycle;\ncomp->audio_level = next_level;\nif (target_sample_index\
      \ >= comp->audio_sample_cursor) comp->audio_sample_cursor = target_sample_index + 1u;\n"
coding:
  headers:
  - pasm_overlay.h
  include_paths:
  - ../include
  linked_libraries: []
  library_paths: []
